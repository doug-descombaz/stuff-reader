# Stuff Reader

A Flutter application for MacOS that reads words from a dictionary database and recites them using text-to-speech.

## Folder Structure

- `lib/`: Main source code.
- `assets/`: Contains the `dictionary.db` database file.
- `test/`: Unit and widget tests.
- `bin/`: Utility scripts for database inspection.
