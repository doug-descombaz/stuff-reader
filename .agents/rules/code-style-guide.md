---
trigger: always_on
---

* Keep tests in parallel directories.
* Always provide public documentation.
* Clearly designate which function(s) of a class. 
* Factor out independent code stanzas from functions. Make sure the naming and documentation of these ad hoc functions clearly makes their relationship to the core functions.
* If a file has multiple classes in it, document them all at the top of the file. 
